import { View, StyleSheet, Text } from 'react-native';

const DetailRearerScreen = () => {
  return (
    <View>
      <Text>유저디테일</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 20,
  },
});

export default DetailRearerScreen;
